import OpenAI from "openai";

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

export async function generatePromo(productType) {
  const prompt = `
შექმენი სარეკლამო აქციის ტექსტი ASTROMAN-ისთვის.

პროდუქტი: ${productType}

მოიცავდეს:
⏰ სიმწირის ელემენტი (შეზღუდული დრო ან რაოდენობა)
🎁 სპეციალური შეთავაზება ან ბონუსი
💥 ძლიერი გახსნა (hook)
✅ 3 ძირითადი სარგებელი
📲 CTA (შეუკვეთე, დაგვიკავშირდი, და სხვ.)
#️⃣ 5-7 ჰეშთეგი

სტილი: გამყიდველი, ენერგიული, ქართული ბაზრისთვის.
`;

  const response = await openai.chat.completions.create({
    model: "gpt-4o",
    temperature: 0.85,
    messages: [
      {
        role: "system",
        content: "შენ ხარ ASTROMAN-ის მარკეტინგ კოპირაიტერი. წერ ქართულად, ემოჯიებით, ძლიერი გაყიდვის ენით."
      },
      { role: "user", content: prompt }
    ]
  });

  return response.choices[0].message.content;
}
